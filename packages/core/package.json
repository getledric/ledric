{
  "name": "@ledric/core",
  "version": "0.0.0",
  "description": "ledric core: schema engine, versioning, refs, validation, ACL. DB-agnostic.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ledric/schema": "workspace:*",
    "@ledric/storage": "workspace:*"
  }
}
